[project]
name = "forex-sentiment-agent"
version = "0.0.0"
description = "A Managed Deep Agent project for EURUSD forex sentiment."
requires-python = ">=3.11"
dependencies = [
    "managed-deepagents==0.5.3",
    "httpx>=0.27.0",
    "langchain-core>=1.0.0",
    "pydantic>=2.0.0"
]
